% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{run_block_hier}
\alias{run_block_hier}
\title{Native hierarchical partial-pooling sampler over a panel of tracks.}
\usage{
run_block_hier(
  n_ind,
  knots_per_ind,
  knot_obs_start,
  knot_obs_len,
  obs_times,
  obs_light,
  cal,
  lp,
  surr_mu,
  surr_p,
  cinv,
  start_lon,
  start_lat,
  end_lon,
  end_lat,
  aux_flat,
  aux_ncol,
  aux_nrow,
  aux_lon0,
  aux_dlon,
  aux_lat0,
  aux_dlat,
  a_pop,
  g0,
  h0,
  block_len,
  sweeps,
  burn,
  thin,
  polish,
  spherical,
  crw,
  rho_prior_sd,
  rho_max,
  shade_ratio,
  seed
)
}
\arguments{
\item{n_ind}{Number of individuals}

\item{knots_per_ind}{Knot count per individual (length n_ind)}

\item{knot_obs_start}{Per-knot global obs start index (length sum knots_per_ind)}

\item{knot_obs_len}{Per-knot obs count}

\item{obs_times}{Global concatenated observation timestamps}

\item{obs_light}{Global concatenated observed light}

\item{cal}{Per-individual response parameters, packed contiguously: length
2*n_ind for the clamped-linear model or 4*n_ind for the logistic one.}

\item{lp}{Per-individual c(lambda, max_light, prob_slab), length 3*n_ind}

\item{surr_mu}{Per-knot surrogate mean (2 per knot)}

\item{surr_p}{Per-knot surrogate 2x2 precision (4 per knot)}

\item{cinv}{Per-individual movement shape precision Cinv (2x2), length 4*n_ind}

\item{start_lon}{Per-individual fixed first-knot (deploy) longitude}

\item{start_lat}{Per-individual fixed first-knot (deploy) latitude}

\item{end_lon}{Per-individual fixed last-knot (retrieval) longitude; NaN = free}

\item{end_lat}{Per-individual fixed last-knot (retrieval) latitude; NaN = free}

\item{aux_flat}{Concatenated per-tag additive location log-likelihood (sensor
terms), each tag a K*ncell block on its own regular lon/lat grid; empty for none}

\item{aux_ncol, aux_nrow}{Per-tag aux grid dimensions (0 columns/rows = no terms)}

\item{aux_lon0, aux_dlon, aux_lat0, aux_dlat}{Per-tag aux grid origin and spacing}

\item{a_pop}{InvGamma shape for sig2_i}

\item{g0}{Gamma shape hyperprior for beta}

\item{h0}{Gamma rate hyperprior for beta}

\item{block_len}{Maximum block length}

\item{sweeps}{Total sweeps}

\item{burn}{Burn-in sweeps}

\item{thin}{Thinning interval}

\item{polish}{Whether to run the red-black polish each sweep}

\item{spherical}{Great-circle movement metric (exp(-gcdist^2/2 sig2)) if TRUE,
else the flat tangent-plane metric at each tag's reference latitude}

\item{crw}{Correlated random walk: model the increments as an AR(1),
delta_t = rho*delta_{t-1} + eps, with a per-tag persistence rho sampled
alongside the movement variance. FALSE gives the memoryless Brownian walk
and is bit-identical to the previous behaviour.}

\item{rho_prior_sd}{Standard deviation of the mean-zero normal prior on each
tag's persistence}

\item{rho_max}{Persistence is confined to (-rho_max, rho_max). Values near 1
are near-non-stationary: displacement then grows like n^1.5 rather than
sqrt(n), so a track can leave the surrogate mesh entirely.}

\item{seed}{RNG seed; 0 means entropy}
}
\value{
List with beta and sig2 (kept draws), plus per-knot track posterior
  mean_lon/sd_lon/mean_lat/sd_lat (concatenated across individuals, same order
  as knots_per_ind), and rho (per-tag persistence draws, zero when crw is FALSE)
}
\description{
Runs the whole Gibbs sweep natively: each individual's track is updated with
the block+polish kernel conditional on its movement variance sig2_i, then
sig2_i and the population scale beta are drawn from their conjugate
full-conditionals. All arrays concatenate the individuals; knots_per_ind gives
each individual's knot count, and knot_obs_start indexes the GLOBAL obs arrays.
}
