% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor_rules.R
\name{ceiling_rule}
\alias{ceiling_rule}
\title{Ceiling rule: a one-sided constraint from an upper bounding surface}
\usage{
ceiling_rule(sd = NULL)
}
\arguments{
\item{sd}{Tolerance for the soft constraint (`NULL` for a hard cut).}
}
\value{
A rule closure of class `tf_rule`. Missing `obs` or `NA` in `expected`
  contribute `0` (uninformative).
}
\description{
Mirror of [floor_rule()]: for sensors where the observed value is an
*upper bound* on the expected field at the true location. A cell is infeasible
when `expected > obs` (i.e. the field value pokes above the observation).
The deficit is `pmax(0, expected - obs)`.
}
\examples{
cr <- ceiling_rule()
cr(obs = 100, expected = c(50, 100, 200))  # 0, 0, -Inf
}
\seealso{
[floor_rule()]
}
