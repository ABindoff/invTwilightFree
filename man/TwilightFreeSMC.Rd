% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smc.R
\name{TwilightFreeSMC}
\alias{TwilightFreeSMC}
\title{Reconstruct animal tracks using a Sequential Monte Carlo (Particle Filter)}
\usage{
TwilightFreeSMC(
  date_time,
  light,
  start_time = NULL,
  end_time = NULL,
  n_particles = 1000,
  start_lat,
  start_lon,
  end_lat = NA_real_,
  end_lon = NA_real_,
  method = c("guided", "ffbs", "forward"),
  step_hours = 12,
  diffusion = 50,
  trans_prob = NULL,
  calibration = NULL,
  likelihood_params = NULL,
  spatial_mask = NULL,
  seed = NULL
)
}
\arguments{
\item{date_time}{A vector of POSIXct dates}

\item{light}{A vector of light observations}

\item{start_time}{Optional start date-time (POSIXct). Data before this time will be ignored.}

\item{end_time}{Optional end date-time (POSIXct). Data after this time will be ignored.}

\item{n_particles}{Number of particles (default 1000)}

\item{start_lat}{Initial latitude (Decimal Degrees)}

\item{start_lon}{Initial longitude (Decimal Degrees)}

\item{end_lat}{Final latitude (optional, defaults to NA)}

\item{end_lon}{Final longitude (optional, defaults to NA)}

\item{method}{Track reconstruction method: "guided" (fast Brownian Bridge), "ffbs" (rigorous Forward-Filtering Backward-Smoothing), or "forward" (standard SMC).}

\item{step_hours}{Time step for the particle filter knots in hours (default 12.0). Movement is proposed every `step_hours`, while likelihood is evaluated continuously.}

\item{diffusion}{Movement diffusion in km/sqrt(day). Can be a vector of state-specific diffusions (e.g., c(10, 100)) to enable behavioral state-switching (HMM).}

\item{trans_prob}{Optional transition probability matrix (flattened) for behavioral states. Defaults to 0.9 diagonal if multiple diffusions are provided.}

\item{calibration}{Calibration parameters c(intercept, slope) mapping zenith to light. If NULL, auto-calibrates from data.}

\item{likelihood_params}{Likelihood parameters c(lambda, max_light, prob_slab). If NULL, auto-calibrates from data.}

\item{spatial_mask}{Optional `RasterLayer` (from the `raster` package) used to constrain particles to valid habitat (e.g. sea vs land). Cells with value 0 (or `NA`) are treated as impassable. If `NULL`, no spatial constraint is applied.}

\item{seed}{Integer seed for reproducibility; if `NULL` the engine is non-deterministic. Pass the same integer to reproduce identical tracks. Note that `TwilightFreeGrid` uses a deterministic HMM and needs no seed.}
}
\value{
A `TwilightFreeTrack` object
}
\description{
This reimagined approach uses all light data and a spike-and-slab likelihood 
to jointly estimate locations and handle non-solar light noise.
}
