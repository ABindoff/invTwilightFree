% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dual_time.R
\name{eval_logpt_loc}
\alias{eval_logpt_loc}
\title{Dual light log-likelihood at a fixed location over a grid of time offsets}
\usage{
eval_logpt_loc(
  lon,
  lat,
  times,
  light,
  offsets = seq(-3 * 3600, 3 * 3600, by = 120),
  calibration,
  likelihood_params
)
}
\arguments{
\item{lon, lat}{Scalar candidate location (decimal degrees).}

\item{times}{Observation times (POSIXct or numeric Unix seconds).}

\item{light}{Observed light values (same length as `times`).}

\item{offsets}{Time offsets to evaluate, in seconds. Default a grid of
+/- 3 hours at 2-minute steps.}

\item{calibration}{`c(intercept, slope)` mapping zenith to expected light.}

\item{likelihood_params}{`c(lambda, max_light, prob_slab)`.}
}
\value{
A data frame with columns `offset` (seconds) and `loglik`.
}
\description{
For a single candidate location, evaluates the spike-and-slab light
log-likelihood as a function of a time offset `tau` added to the observation
clock. This is the transpose of [eval_logpk_grid()]: location fixed, time
varied. A sharp peak in `tau` locates the solar time the light implies at this
location; against the known clock it is the clock error (and, equivalently,
the longitude line of position expressed in time).
}
\examples{
\dontrun{
d <- eval_logpt_loc(150, -50, track$time, track$light,
                    calibration = c(64, 64/90), likelihood_params = c(0.5, 64, 0.05))
d$offset[which.max(d$loglik)]   # clock error implied at (150, -50), seconds
}
}
\seealso{
[calibrate_clock()], [eval_logpk_grid()]
}
