% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dual_time.R
\name{calibrate_clock_from_endpoints}
\alias{calibrate_clock_from_endpoints}
\title{Estimate the clock correction from the known deployment and retrieval fixes}
\usage{
calibrate_clock_from_endpoints(
  date_time,
  light,
  start_lon,
  start_lat,
  end_lon,
  end_lat,
  calibration = NULL,
  likelihood_params = NULL,
  window_days = 3,
  search = 2 * 3600,
  by = 60
)
}
\arguments{
\item{date_time}{Observation times (POSIXct).}

\item{light}{Observed light values.}

\item{start_lon, start_lat, end_lon, end_lat}{Known release and recovery
locations (decimal degrees).}

\item{calibration, likelihood_params}{As in [eval_logpt_loc()].}

\item{window_days}{Days of light around each fix used for calibration.}

\item{search}{Half-width of the offset search, seconds (default 2 h).}

\item{by}{Offset step, seconds (default 60).}
}
\value{
A clock object as returned by [calibrate_clock()], or `NULL`.
}
\description{
Engine-agnostic front end: clock calibration is a transform on the observation
timestamps, computed before any engine runs, so the same correction serves
[TwilightFreeSMC()] and [TwilightFreeGrid()] identically (both expose it via
`calibrate = TRUE`). Returns `NULL` with a warning if either endpoint location
is unknown. If `calibration`/`likelihood_params` are not supplied, a rough
light calibration is derived internally, which is adequate because the clock
estimate depends on twilight timing rather than absolute light scale.
}
\seealso{
[calibrate_clock()], [eval_logpt_loc()]
}
