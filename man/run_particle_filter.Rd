% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{run_particle_filter}
\alias{run_particle_filter}
\title{Run the Particle Filter Engine}
\usage{
run_particle_filter(
  unix_times,
  obs_light,
  n_particles,
  start_lat,
  start_lon,
  end_lat,
  end_lon,
  method,
  step_hours,
  diffusion,
  trans_prob,
  calibration,
  likelihood_params,
  shade_ratio,
  mask_matrix,
  mask_extent,
  mask_nrow,
  mask_ncol,
  seed,
  aux_logl_flat,
  aux_extent,
  aux_nrow,
  aux_ncol,
  flat_light_threshold,
  flat_light_scale
)
}
\arguments{
\item{unix_times}{Timestamps of observations}

\item{obs_light}{Observed light values}

\item{n_particles}{Number of particles to use}

\item{start_lat}{Starting Latitude}

\item{start_lon}{Starting Longitude}

\item{end_lat}{Ending Latitude (use NA if unknown)}

\item{end_lon}{Ending Longitude (use NA if unknown)}

\item{method}{Method to use: "guided", "ffbs", or "forward"}

\item{step_hours}{Hours between particle movement steps}

\item{diffusion}{Diffusion coefficient (kilometers per sqrt(day))}

\item{trans_prob}{Flattened row-major transition probability matrix for behavioral states}

\item{calibration}{Response parameters: `c(intercept, slope)` for the clamped-linear
model, or `c(floor, amp, z50, scale)` for the logistic model (see details in
`fit_light_response`). Length selects the model.}

\item{likelihood_params}{c(lambda, max_light, prob_slab)}

\item{shade_ratio}{Ratio of the upper-arm decay rate to the shading rate,
i.e. lam_hi / lam_lo. 2 reproduces the previous fixed behaviour. Values
below 1 make the shading tail heavier, which is what a continuously
diving animal needs: most of its record is far below the clear-sky
expectation and should be cheap to explain.}

\item{mask_matrix}{Flattened spatial mask matrix (0 = impassable); empty for no mask}

\item{mask_extent}{c(xmin, xmax, ymin, ymax) extent of the mask raster}

\item{mask_nrow}{Number of rows in the mask raster}

\item{mask_ncol}{Number of columns in the mask raster}

\item{seed}{Integer seed for reproducibility; 0 means non-deterministic (uses entropy)}

\item{aux_logl_flat}{Flattened aux log-likelihood raster (k_steps * nrow * ncol, row-major); empty for no aux}

\item{aux_extent}{c(xmin, xmax, ymin, ymax) extent of the aux raster}

\item{aux_nrow}{Number of rows in the aux raster}

\item{aux_ncol}{Number of columns in the aux raster}

\item{flat_light_threshold}{Light-range threshold (max_obs - min_obs) below which a knot is
classified as informationally flat (no visible twilight). Diffusion is scaled by
`flat_light_scale` on such knots to prevent unconstrained wandering when the light
carries no positional signal. Default 10.0 (raw light units).}

\item{flat_light_scale}{Multiplicative scale applied to the diffusion coefficient on flat-light
knots (see `flat_light_threshold`). Values in (0, 1) constrain movement; 1.0 disables the
heuristic. Default 0.1 (10x tighter).}
}
\description{
Core SMC engine for TwilightFree track reconstruction.
}
